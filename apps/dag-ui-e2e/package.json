{
  "name": "@onepipeline-ui/dag-ui-e2e",
  "version": "0.0.0-workspace",
  "private": true,
  "type": "module",
  "//": "sibling dependencies are `*`, never `workspace:*`: npm 11.17.0 exits EUNSUPPORTEDPROTOCOL on `npm install` and on `npm ci`, which is how scripts/workspace-install.sh installs this repository, and normalises the protocol out of package-lock.json besides. tests/packaging.rs fails the build on it.",
  "dependencies": {
    "@onepipeline-ui/timeline-categories": "*",
    "zod": "4.1.12"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
