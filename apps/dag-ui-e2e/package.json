{
  "name": "@onepipeline-ui/dag-ui-e2e",
  "version": "0.0.0-workspace",
  "private": true,
  "type": "module",
  "//": "sibling dependencies are `*`, never `workspace:*`: npm 11 exits EUNSUPPORTEDPROTOCOL on `npm ci`, which is how scripts/workspace-install.sh installs this repository, and normalises the protocol out of the lockfile besides. tests/packaging.rs fails the build on it.",
  "dependencies": {
    "@onepipeline-ui/dag-ui": "*",
    "@onepipeline-ui/timeline-categories": "*",
    "zod": "4.1.12"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
